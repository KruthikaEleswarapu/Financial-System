server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://account-db:3306/accounts
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

management:
  endpoints:
    web:
      exposure:
        include: "*"
